/**
 * 文档转换后端 API 地址配置
 * =========================
 * ⚠️ 后端（Python + LibreOffice）部署到 VPS 后，请将 toolbox-api.cc.cd
 *    的 DNS 指向后端服务器（或在 CF 里为后端配置 HTTPS 域名），
 *    然后把下方 API_BASE 改为实际地址。
 */
export const API_BASE = 'https://toolbox-api.cc.cd'
